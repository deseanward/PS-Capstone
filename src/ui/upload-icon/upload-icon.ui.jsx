import React from "react";
import { UploadIconContainer } from "./upload-icon.ui.styles";

const UploadIcon = ({ children }) => {
  return <UploadIconContainer>{children}</UploadIconContainer>;
};

export default UploadIcon;
